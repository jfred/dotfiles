// Place your key bindings in this file to override the defaults
[
    {
        "key": "ctrl+shift+1",
        "command": "linear-open-issue.openIssue"
    },
    {
        // Custom keybinding to cycle test DB modes, mostly for django projects
        "key": "cmd+; d",
        "command": "settings.cycle.testDbMode",
        // "when": "env.MANAGE_PY"
    }
]